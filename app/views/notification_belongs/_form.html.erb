<%= form_with(model: notification_belong) do |form| %>
  <% if notification_belong.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(notification_belong.errors.count, "error") %> prohibited this notification_belong from being saved:</h2>

      <ul>
        <% notification_belong.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :readed %>
    <%= form.check_box :readed %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :notification_id %>
    <%= form.text_field :notification_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
